<UserControl x:Class="SuperCV.MySwitch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
    >
    <Border Background="Transparent"
            Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Height="{Binding HeightValue, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid>
            <!-- 开关背景轨道 -->
            <Border x:Name="TrackBorder" CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Background="Transparent">
            </Border>

            <!-- 开关滑块 -->
            <Border x:Name="ThumbBorder"
                    Width="{Binding ThumbSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Height="{Binding ThumbSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CornerRadius="{Binding ThumbCornerRadius, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    HorizontalAlignment="Left" Background="Transparent"
                    RenderTransformOrigin="0.5,0.5" Margin="0">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="ThumbTransform"/>
                </Border.RenderTransform>
            </Border>

            <!-- 点击区域 -->
            <Border Background="Transparent" MouseLeftButtonDown="OnMouseLeftButtonDown" 
                    MouseEnter="Border_MouseEnter" MouseLeave="Border_MouseLeave"/>
        </Grid>
    </Border>
</UserControl>